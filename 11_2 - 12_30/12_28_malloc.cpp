#include <stdio.h>
#include <stdlib.h>
int main()
{
    // int number;
    // int* a;
    // int i;
    // printf("Enter the amount: ");
    // scanf("%d",&number);
    // //malloc返回的结果是void*,所以转换成(int*)
    // a = (int*)malloc(number*sizeof(int));//就可把a当数组用了
    // for ( i = 0; i < number; i++)
    // {
    //     scanf("%d",&a[i]);
    // }
    // for ( i = number-1; i >=0 ; i--)
    // {
    //     printf("%d",a[i]);
    // }
    // free(a);//把申请来的空间还给“系统”
    // printf(",");

    // void *p; //有malloc就记住要free！！！
    // int cnt=0;
    // while((p=malloc(100*1024*1024))){
    //     cnt++;
    // }
    // printf("分配了%d00MB空间\n",cnt);
    // free(p);

    return 0;
}
